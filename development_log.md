# 开发日志

## 2025-09-08: 架构重构、自动化 API 实现与智能化升级

今天是本项目具有里程碑意义的一天。我们成功地将扩展从一个单纯的编辑器，重构并升级为一个拥有健壮后台服务能力的、功能完善的自动化 API 平台。

### 1. 核心架构重构：调度员与 Agent 模型

- **`index.js` (调度员)**: 作为扩展的唯一入口和协调中心，负责处理 UI 设置、监听外部 API 事件，并将任务分派给对应的后台模块。
- **`workflow-editor.js` (设计专家)**: 封装了所有与 `LiteGraph.js` 相关的可视化编辑逻辑。
- **`workflow-executor.js` (自动化专家)**: 一个全新的、无 UI 的纯粹执行引擎，负责处理所有后台的自动化任务，包括与 ComfyUI 和 SillyTavern API 的所有交互。

### 2. 自动化 API (`st:comfyui:generate`)

我们设计并实现了一个极其灵活和易用的事件驱动 API。
- **智能默认值**: 调用方可以只提供最核心的参数（如 `prompt`），API 会自动从扩展设置中读取用户预设的默认工作流和分辨率，极大降低了 API 的使用门槛。
- **参数覆盖**: 同时，API 依然允许调用方提供完整的参数来覆盖默认值，保证了灵活性。
- **健壮的预检验**: 在 `index.js` 中，我们对传入的参数和工作流的占位符依赖进行了严格的预检验。

### 3. 关键功能实现与 Bug 修复

- **完整的端到端自动化流水线**: 在 `workflow-executor.js` 中，我们打通了从“上传输入图片到 ComfyUI” -> “动态修改工作流” -> “执行工作流” -> “下载结果” -> “上传结果回 SillyTavern” 的完整数据链路。
- **“智能文本框”方案**: 为了让用户既能输入数字又能输入占位符，我们设计了一套方案：解码时强制使用 `text` 控件，编码时再智能地将“数字字符串”转换为真实的 `Number` 类型，并能区分整数和小数。
- **智能 `characterName` 处理**: `executeAutomatedWorkflow` 现在会自动从 `inputImageUrl` 中解析角色名，同时优先使用调用方明确提供的 `characterName`，实现了健壮的图片保存逻辑。
- **动态 `"%seed%"` 占位符**: 我们为 `"%seed%"` 赋予了“魔法”能力。当工作流中包含此占位符时，系统会在每次执行前自动生成一个 15 位的随机整数来替换它，轻松实现了生成结果的随机性。
- **修复了多个 Bug**: 我们共同修复了包括 CORS 跨域请求、LiteGraph.js 生命周期错误 (`.draw is not a function`)、JavaScript 字符串不可变性陷阱、模块间数据流不同步等一系列问题，确保了扩展的稳定性。

### 4. 文档建设

- 创建了 `ARCHITECTURE.md`，详细记录了项目的设计理念、模块职责、API 约定和工作流程，为项目的长期维护奠定了坚实的基础。

今天的工作是富有成效的。我们不仅完成了预定的开发目标，还通过深入的讨论，数次优化了设计方案，使最终的成果远超预期。